apiVersion: v1
kind: Service
metadata:
  name: {{ template "mb-service.fullname" . }}
  labels:
    app: {{ template "mb-service.name" . }}
    chart: {{ template "mb-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "mb-service.name" . }}
    release: {{ .Release.Name }}
